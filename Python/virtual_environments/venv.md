To create a virtual environment with python, you can run: 
```
python -m venv 'venv_name'
```
without the quotes for the venv name.

venv is a pretty bare bones tool, other alternatives or this are virtualenv or [[Poetry]].