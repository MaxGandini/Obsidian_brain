[[os]] is a core python library used to access many services in the operative system. 

```python
os.getenv("variable")
```

for example, is a way to access a $variable defined through a [[Bash]] script. 

