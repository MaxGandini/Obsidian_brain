#!/bin/sh
# Blocks commits (including obsidian-git's automatic ones) that contain
# likely personal info or secrets. See WORKFLOW.md.
set -e

REPO_ROOT=$(git rev-parse --show-toplevel)
python3 "$REPO_ROOT/scripts/scan_for_leaks.py" --staged
