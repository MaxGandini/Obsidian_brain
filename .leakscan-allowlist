# Regex allowlist for scripts/scan_for_leaks.py
# One regex per line, matched against the full line of text. Lines matching
# any of these are skipped entirely. Add a comment above each entry
# explaining why it's a false positive, not a real leak.

# Generic placeholder emails already used throughout the vault as examples.
john\.doe@(example|contoso)\.com
proxyUsername:proxyPassword@proxy\.server\.com

# Ed Donner's public course-instructor email, present in vendored LLM course
# notebook boilerplate (AI/Generative_AI/LLM/LLM_course) — not vault-owner PII.
ed@edwarddonner\.com

# Public academic contact for the vendored Bike Sharing dataset README
# (Fanaee-T & Gama, Prophet time-series course materials) — not vault-owner PII.
hadi\.fanaee@fe\.up\.pt
